env: "local" #local, dev, prod

storage_path: "./storage/storage.db"

http_server:
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s
  username: "vlad"

db:
  user: "postgres"
  host: "localhost"
  port: "5432"
  dbname: "postgres"
  sslmode: "disable"
